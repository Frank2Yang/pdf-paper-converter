# Vercel 部署专用依赖文件
# 精简版本，适合 Vercel 的资源限制

# Streamlit 核心
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0,<2.0.0

# HTTP 请求
requests>=2.28.0

# 系统信息
psutil>=5.9.0

# PDF 处理（轻量级）
PyMuPDF>=1.23.0
pdfplumber>=0.9.0

# 图像处理（轻量级）
Pillow>=9.5.0

# Markdown 处理
markdown>=3.4.0

# 注意：MinerU 完整版本过大，无法在 Vercel 上部署
# 在 Vercel 上将使用演示模式或 API 调用方式
