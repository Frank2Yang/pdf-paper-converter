# MinerU PDF 解析器 - Vercel 部署依赖（精简版）

# Streamlit 核心框架
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0,<2.0.0

# HTTP 请求和系统工具
requests>=2.28.0

# PDF 处理（轻量级）
PyMuPDF>=1.23.0
pdfplumber>=0.9.0

# 图像处理（轻量级）
Pillow>=9.5.0

# Markdown 处理
markdown>=3.4.0

# 文件处理
python-multipart>=0.0.6

# 日期时间处理
python-dateutil>=2.8.0

# 注意：完整版 MinerU 依赖包过大，可能超出 Vercel 限制
# 如需完整功能，请考虑以下选项：
# 1. 使用 Vercel Pro 账号（更大的资源限制）
# 2. 部署 MinerU API 到其他服务器，通过 API 调用
# 3. 使用其他支持大型依赖的平台（如 Railway、Render）

# 完整版依赖（注释掉以避免部署失败）：
# mineru>=2.1.0
# torch>=2.0.0,<2.2.0
# torchvision>=0.15.0,<0.17.0
# transformers>=4.30.0,<5.0.0
# rapid-table==1.0.5
# onnxruntime>=1.15.0,<1.17.0
# opencv-python-headless>=4.8.0
# shapely>=2.0.0
# pyclipper>=1.3.0
# scikit-image>=0.20.0
# matplotlib>=3.6.0
# scipy>=1.10.0
